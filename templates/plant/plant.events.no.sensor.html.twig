<h5>Events without sensor | All events count {{ plant.events | length }}</h5>
<hr/>
{% apply spaceless %}
<div class="table-responsive-lg">
    <table id="feedEventTable" class="display nowrap" style="width:100%">
        <thead>
        <tr>
            <th style="font-size: 8px;">ID</th>
            <th>Day</th>
            <th>Week</th>
            <th>Flower</th>
            <th>Name</th>
            <th>Note</th>
            <th>Water</th>
            <th>TDS</th>
            <th>Value</th>
            <th>Time 1</th>
            <th>Time 2</th>
            <th>Type</th>
            <th>PH</th>
        </tr>
        </thead>
        <tbody>
        {% set counter = 0 %}
        {% for event in plant.events %}
            {% if counter > 1000 %}
            {% else %}
                {% if event.sensor is none %}
                    {% set counter = counter+1 %}
                    {% set type = event.type | parseType %}
                    <tr>
                        {% if type == "Feed"  %}{% set _url = path('events_event_feed_show', {'id': event.id}) %}
                        {% else %}{% set _url = path('event_show', {'id': event.id}) %}{% endif %}
                        <td style="font-size: 9px;">{{ event.id }}</td>
                        <td>{{ event.day(plant) }}</td>
                        <td>{{ event.week(plant) }}</td>
                        <td>{% if event.inFlower %} {{ event.dayInFlower(plant) }} / {{ event.weekInFlower(plant) }} {% endif %}</td>
                        <td><a href="{{ _url }}">{{ event.name | length > 0 ? event.name : event.id}}</a></td>
                        <td>{{ event.note }}</td>
                        <td>{% if type == "Feed"  %}
                            <span class="blue">{{ event.water }}</span>
                            {% endif %}
                        </td>
                        <td>{% if type == "Feed"  %}
                                <span class="blue">{{ event.tds }}</span>
                            {% endif %}
                        </td>
                        <td>{% if type == "Feed"  %}
                                Ph: {{ event.ph }} | EC:{{ event.ec }}
                            {% else %}
                                {{ event.value }}
                            {% endif %}
                        </td>
                        <td>{{ event.happenedAt | time_diff }}</td>
                        <td title="{{ event.happenedAt | date('Y/m/d H:i:s') }}">{{ event.happenedAt | date('d H:i') }}</td>
                        <td>{{ event.type | parseType }}</td>
                        <td>{% if type == "Feed"  %}<span class="blue">{{ event.ph }}</span>{% endif %}
                        </td>
                    </tr>
                {% endif %}
            {% endif %}
        {% endfor %}
        </tbody>
    </table>
</div>
{% endapply %}


<script>
    $(document).ready(function() {
        $('#feedEventTable').DataTable( {
            "columnDefs": [
                {
                    "targets": [ 9, 10 ],
                    "searchable": false
                },
                {
                    "targets": [ 11 , 12],
                    "visible": false
                }
            ],
            "pageLength": 25,
            order: [[0, 'desc']],
            rowGroup: {
                endRender: function ( rows, group ) {
                    var TdsRatio = 0;
                    var PhRatio = 0;
                    rows.data().each(
                        function ( group, i ) {
                            console.log(group);
                            console.log(group);
                            var water = $(group[6]).text();
                            var tds = $(group[7]).text();
                            var ph = $(group[12]).text();
                            TdsRatio += water * tds;
                            PhRatio += water * ph;
                        }
                    );
                    var amountOfWater = rows
                        .data()
                        .pluck(6)
                        .reduce( function (a, b) {
                            return a + $(b).text().replace(/[^\d|\.]/g, '')*1;
                        }, 0);

                    var phValue = PhRatio/amountOfWater;
                    var phVColor = 'label-success';
                    if (phValue < 5.7 || phValue > 6.4) {
                        phVColor = 'label-info';
                    }
                    if (phValue < 5.5 || phValue > 6.5) {
                        phVColor = 'label-danger';
                    }
                    return 'Week ['+group+'] ' +
                        'Liters: <strong class="green">'+  $.fn.dataTable.render.number(',', '.', 1, ' ').display(amountOfWater) + ' L</strong>' +
                        ' - ' +
                        '<strong>Total TDS</strong> <strong class="blue">'  + $.fn.dataTable.render.number(',', '.', 0, '').display(TdsRatio) + ' PPM</strong>' +
                        ' - ' +
                        '<strong>EC</strong> <strong class="brown">'  + $.fn.dataTable.render.number(',', '.', 1, '').display(TdsRatio*2/amountOfWater/1000) + ' AVG</strong>' +
                        ' - ' +
                        '<span class="label ' + phVColor + ' label-white middle">PH ' + $.fn.dataTable.render.number(',', '.', 2, '').display(phValue) + '</span> AVG' +
                        '';
                },
                dataSrc: 2
            }
        } );
    } );
</script>
